apiVersion: v1
kind: Service
metadata:
  name: muskhkhxxa
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "muskhkhxxa"
    port: 8080
  selector:
    app: muskhkhxxa
